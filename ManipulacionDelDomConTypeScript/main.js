(function main() {
    var app = document.querySelector('.app');
    if (app) {
        app.textContent = "hola mundo manipulaicon del DOM con typeScript, un gusto iniciar este nuevo proyecto";
    }
})();
